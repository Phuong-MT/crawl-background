import fs from "fs/promises"
import path from "path"
// async function main(){
//   let data  = await fs.readFile(path.join(process.cwd(), "freepik-result.json"), "utf-8");
//   data = JSON.parse(data);
//   for(const req in data){
//       const urls = data[req];
//       for(const url of urls){
//         console.log(url)
//       }
//   }
// }
// main().catch(e=>console.log(e))

async function main(){
  
}